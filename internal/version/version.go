// Package version holds the SDK version, shared by the root package's public
// Version constant and the transport's default User-Agent so the two cannot
// drift apart. Bump this when cutting a release tag.
package version

// Version is the SDK release version, without the leading "v".
const Version = "0.1.3"
